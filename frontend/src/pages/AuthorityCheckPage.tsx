import { MainLayout } from '@/components/layout/MainLayout';
import { Box, Typography } from '@mui/material';

export const AuthorityCheckPage = () => {
  return (
    <MainLayout>
      <Box>
        <Typography>権限チェック画面</Typography>
      </Box>
    </MainLayout>
  );
};
